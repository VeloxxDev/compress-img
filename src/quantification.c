#include <stdio.h>
#include <stdlib.h>

typedef struct histo_iter {
    int R, G;
    cell current;
} *histo_iter;

void quantification(histo h,int* tab,int K) {
    int i,j;

    list = create_cell(h,h->)
        

}
